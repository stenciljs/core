name: 'Install Playwright Browsers'
description: 'Installs Playwright browsers with caching'
runs:
  using: composite
  steps:
    - name: Get Playwright version
      id: playwright-version
      run: echo "version=$(pnpm --filter @stencil-core-tests/ssr exec playwright --version)" >> $GITHUB_OUTPUT
      shell: bash

    - name: Cache Playwright browsers
      uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
      id: playwright-cache
      with:
        path: |
          ~/.cache/ms-playwright
          ~/Library/Caches/ms-playwright
          ~/AppData/Local/ms-playwright
        key: playwright-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}

    - name: Install Playwright Browsers
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      run: pnpm --filter "@stencil-core-tests/ssr" exec playwright install --with-deps
      shell: bash

    - name: Install Playwright system deps (cache hit)
      if: steps.playwright-cache.outputs.cache-hit == 'true' && runner.os == 'Linux'
      run: pnpm --filter "@stencil-core-tests/ssr" exec playwright install-deps
      shell: bash
