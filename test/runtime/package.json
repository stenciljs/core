{
  "name": "@stencil-core-tests/runtime",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "module": "dist/lazy/index.js",
  "types": "dist/types/components.d.ts",
  "scripts": {
    "build": "stencil build",
    "build:external": "pnpm --filter @stencil-core-tests/runtime-external build",
    "test": "pnpm build:external && stencil-test",
    "test:watch": "stencil-test --watch",
    "test:dist": "pnpm build:external && stencil-test --project lazy",
    "test:custom-elements": "pnpm build:external && stencil-test --project standalone"
  },
  "dependencies": {
    "@stencil-core-tests/runtime-external": "workspace:*",
    "@stencil/core": "workspace:*"
  },
  "devDependencies": {
    "@stencil/sass": "^3.3.3",
    "@stencil/vitest": "catalog:",
    "@vitest/browser": "catalog:",
    "@vitest/browser-playwright": "catalog:",
    "bootstrap": "^5.3.8",
    "normalize.css": "^8.0.1",
    "vitest": "catalog:"
  },
  "volta": {
    "extends": "../../package.json"
  },
  "collection": "dist/collection/collection-manifest.json"
}
