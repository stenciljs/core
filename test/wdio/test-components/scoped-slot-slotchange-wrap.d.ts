import type { Components, JSX } from "../dist/types/components";

interface ScopedSlotSlotchangeWrap extends Components.ScopedSlotSlotchangeWrap, HTMLElement {}
export const ScopedSlotSlotchangeWrap: {
    prototype: ScopedSlotSlotchangeWrap;
    new (): ScopedSlotSlotchangeWrap;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
