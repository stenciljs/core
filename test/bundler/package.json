{
  "private": true,
  "scripts": {
    "build": "npm run build.component-lib && npm run build.vite",
    "build.component-lib": "cd component-library && npm run build && cd ..",
    "build.vite": "cd vite-bundle-test && npm ci --prefer-online --no-audit --registry=https://registry.npmjs.org/ && npm run build && cd ..",
    "clean": "npm run clean.component-lib && npm run clean.vite",
    "clean.component-lib": "cd component-library && npm run clean && cd ../",
    "clean.vite": "cd vite-bundle-test && npm run clean && cd ../",
    "test": "npm run build && npm run test:run",
    "test:run": "cd vite-bundle-test && jest --config=../jest.config.js vite-bundle.spec.ts",
    "start": "npm run clean && npm run test"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^24.6.2",
    "baseline-browser-mapping": "^2.9.11",
    "jest": "^29.5.0",
    "puppeteer": "^24.1.0",
    "ts-jest": "^29.1.0",
    "typescript": "5.9.2"
  }
}
