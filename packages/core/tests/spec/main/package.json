{
  "name": "@stencil/spec-tests",
  "private": true,
  "type": "module",
  "main": "dist/lazy/index.cjs.js",
  "collection": "dist/lazy/collection/collection-manifest.json",
  "scripts": {
    "build:sibling": "pnpm --filter sibling-spec build",
    "test": "pnpm build:sibling && stencil-test",
    "test:watch": "stencil-test --watch",
    "test:dist": "pnpm build:sibling && stencil-test --project dist",
    "test:custom-elements": "pnpm build:sibling && stencil-test --project custom-elements"
  },
  "dependencies": {
    "@stencil/core": "workspace:*",
    "sibling-spec": "workspace:*"
  },
  "devDependencies": {
    "@stencil/sass": "^3.0.12",
    "@stencil/vitest": "catalog:",
    "@vitest/browser": "catalog:",
    "@vitest/browser-playwright": "catalog:",
    "bootstrap": "^5.3.3",
    "normalize.css": "^8.0.1",
    "vitest": "catalog:"
  }
}
