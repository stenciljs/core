{
  "name": "ionic-app",
  "private": true,
  "version": "1.0.0",
  "description": "ionic-app",
  "license": "MIT",
  "type": "module",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build --debug",
    "build.dev": "stencil build --dev",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil-test && playwright test",
    "test.e2e": "playwright test",
    "test.spec": "stencil-test",
    "generate": "stencil generate"
  },
  "dependencies": {
    "@ionic/core": "^7.0.0",
    "@stencil/core": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@stencil/playwright": "1.2.3-next.11",
    "vitest": "catalog:"
  }
}
