{
  "name": "test-basic",
  "private": true,
  "type": "module",
  "dependencies": {
    "@stencil/core": "workspace:*"
  },
  "scripts": {
    "build": "stencil build --prod",
    "start": "stencil build --dev --watch --serve"
  }
}
