{
  "private": true,
  "scripts": {
    "build": "pnpm -C component-library run build && pnpm -C vite-bundle-test run build",
    "test": "pnpm build && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "serve": "^14.2.4"
  }
}
