{
  "name": "docs-readme-testbed",
  "version": "1.0.0",
  "description": "A test app for the docs-readme output target",
  "files": [
    "dist/"
  ],
  "scripts": {
    "prepare.readmes": "node ../../scripts/test/copy-readme.js",
    "build": "npm run prepare.readmes && stencil build",
    "build.dev": "npm run prepare.readmes &&  stencil build --dev",
    "start": "stencil build --dev --watch --serve",
    "test": "npm run prepare.readmes &&  stencil test --spec --e2e",
    "test.watch": "npm run prepare.readmes && stencil test --spec --e2e --watch",
    "generate": "stencil generate"
  },
  "dependencies": {
    "@stencil/core": "workspace:*"
  },
  "license": "MIT"
}
